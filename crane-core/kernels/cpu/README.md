# CPU native kernels

This directory is the home for future CPU-native kernel sources and their
backend-specific build integration. Until then, CPU execution uses the
portable Rust/Candle implementations in `crane-core/src/ops`.
